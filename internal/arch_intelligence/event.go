package arch_intelligence

// Note: Event types and kinds (e.g., ArchEvent, EventKind) are defined in
// internal/archmodel/model.go as part of the architecture model registry
// to prevent circular dependencies across stages.
// This file serves as a documentation marker for Stage 5 event structures.
